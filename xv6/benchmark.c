#include <stdlib.h>
#include "types.h"
#include "stat.h"
#include "user.h"

// TODO: update with the path of the file being read
#define PATH relative/path/to/file

int main(int argc, char const *argv[]) {
    /* code */
    return 0;
}

float bench_sequential(void) {
    return 0;
}

float bench_random(void) {
    return 0;
}

float bench_weighted_random(void) {
    return 0;
}
